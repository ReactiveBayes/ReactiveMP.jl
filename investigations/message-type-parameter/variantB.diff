diff --git a/src/marginal.jl b/src/marginal.jl
index 86407093..1be8deba 100644
--- a/src/marginal.jl
+++ b/src/marginal.jl
@@ -69,8 +69,8 @@ This is intentional: annotations are out-of-band metadata about *how* a marginal
 not part of the belief it represents. Compare `getannotations` explicitly when you need
 annotation-sensitive equality.
 """
-mutable struct Marginal{D}
-    const data::D
+mutable struct Marginal
+    const data::Any
     const is_clamped::Bool
     const is_initial::Bool
     const annotations::AnnotationDict
@@ -173,7 +173,6 @@ MacroHelpers.@proxy_methods Marginal getdata [
 
 # Eltype is special here, because it should be only defined on types
 # Otherwise it causes invalidations and slower compile times
-Base.eltype(::Type{<:Marginal{D}}) where {D} = Base.eltype(D)
 
 Distributions.mean(fn::Function, marginal::Marginal) =
     mean(fn, getdata(marginal))
@@ -189,7 +188,6 @@ function as_marginal end
 
 as_marginal(marginal::Marginal) = marginal
 
-dropproxytype(::Type{<:Marginal{T}}) where {T} = T
 
 skip_initial() = filter(v -> !is_initial(v))
 skip_clamped() = filter(v -> !is_clamped(v))
diff --git a/src/message.jl b/src/message.jl
index d16427d8..1eaab441 100644
--- a/src/message.jl
+++ b/src/message.jl
@@ -77,8 +77,8 @@ This is intentional: annotations are out-of-band metadata about *how* a message
 not part of the belief the message represents. Compare `getannotations` explicitly when you
 need annotation-sensitive equality.
 """
-mutable struct Message{D} <: AbstractMessage
-    const data::D
+mutable struct Message <: AbstractMessage
+    const data::Any
     const is_clamped::Bool
     const is_initial::Bool
     const annotations::AnnotationDict
@@ -439,7 +439,6 @@ MacroHelpers.@proxy_methods Message getdata [
 
 # Eltype is special here, because it should be only defined on types
 # Otherwise it causes invalidations and slower compile times
-Base.eltype(::Type{<:Message{D}}) where {D} = Base.eltype(D)
 
 Distributions.mean(fn::Function, message::Message) = mean(fn, getdata(message))
 
@@ -498,7 +497,6 @@ function as_message(
     return computed
 end
 
-dropproxytype(::Type{<:Message{T}}) where {T} = T
 
 ## Message observable
 
diff --git a/src/rule_arguments.jl b/src/rule_arguments.jl
index 25b8bf0f..30246f3b 100644
--- a/src/rule_arguments.jl
+++ b/src/rule_arguments.jl
@@ -129,7 +129,9 @@ rule_marginals(f::F, ::Nothing, ::Nothing) where {F} = Marginals(NamedTuple())
 # the methods that existed when it was defined. They are closed; no other method is expected.
 # The block labels of a marginal holding a `FactorizedCluster`, or `nothing`.
 factorized_blocks(::Type) = nothing
-factorized_blocks(::Type{<:Marginal{<:MessagePassingRulesBase.FactorizedCluster{K}}}) where {K} = K
+# Variant B (untyped `Marginal`): the blocks are read off the type of the marginal's data,
+# which only a call with the data themselves can see.
+factorized_blocks(::Type{<:MessagePassingRulesBase.FactorizedCluster{K}}) where {K} = K
 
 # A block's value is its part of the cluster; its annotations are the joint's.
 block_value(::typeof(getdata), marginal, block) = getdata(marginal)[block]
@@ -145,10 +147,20 @@ function partitions(blocks, members)
     return all(block -> issorted(map(member -> findfirst(==(member), members), block)), blocks)
 end
 
+# Variant B: a joint may hold a `FactorizedCluster`, which the untyped `Marginal` no longer
+# says in its type; such calls go through the data's types, a dynamic dispatch.
 @generated function rule_marginals(f::F, ::Val{N}, inputs::Tuple) where {F, N}
+    any(name -> name isa Tuple, N) || return :(rule_marginals(f, Val{N}(), inputs, nothing))
+    return :(rule_marginals(f, Val{N}(), inputs, map(marginal_data, inputs)))
+end
+# Only a `Marginal`'s data can hold the blocks, as with `Marginal{<:FactorizedCluster}` before.
+marginal_data(marginal::Marginal) = getdata(marginal)
+marginal_data(_) = nothing
+
+@generated function rule_marginals(f::F, ::Val{N}, inputs::Tuple, datas) where {F, N}
     singlekeys, singlevalues, jointkeys, jointvalues, i = Symbol[], Any[], Any[], Any[], 1
     for name in N
-        blocks = name isa Tuple ? factorized_blocks(inputs.parameters[i]) : nothing
+        blocks = name isa Tuple ? factorized_blocks(datas.parameters[i]) : nothing
         if blocks !== nothing
             partitions(blocks, name) || return :(
                 throw(
diff --git a/src/score/score.jl b/src/score/score.jl
index e961c485..d5b129ff 100644
--- a/src/score/score.jl
+++ b/src/score/score.jl
@@ -25,11 +25,12 @@ struct KLDivergence end
 
 # A `FactorizedCluster`'s entropy is the sum over its blocks, which BayesBase's
 # `FactorizedJoint` gives; v6 needed a method of its own for its NamedTuple joints.
-score(::DifferentialEntropy, marginal::Marginal) = entropy(marginal)
+score(::DifferentialEntropy, marginal::Marginal) = score_entropy(getdata(marginal))
+score_entropy(data) = entropy(data)
 # A point mass whose point is a distribution, the constant of a prior `x ~ d`, is −∞ like any
 # other, in the distribution's float type; BayesBase would take the distribution's type for it.
-score(::DifferentialEntropy, marginal::Marginal{<:PointMass{<:Distribution}}) =
-    BayesBase.MinusInfinity(paramfloattype(BayesBase.getpointmass(getdata(marginal))))
+score_entropy(data::PointMass{<:Distribution}) =
+    BayesBase.MinusInfinity(paramfloattype(BayesBase.getpointmass(data)))
 
 ## Kl KlDivergence
 
diff --git a/test/variables/variable_tests.jl b/test/variables/variable_tests.jl
index b377f0fa..975d4ed4 100644
--- a/test/variables/variable_tests.jl
+++ b/test/variables/variable_tests.jl
@@ -41,7 +41,7 @@
         subscription = subscribe!(
             get_stream_of_marginals(variable),
             (marginal) -> begin
-                @test typeof(marginal) <: Marginal{T}
+                @test typeof(marginal) <: Marginal && ReactiveMP.typeofdata(marginal) <: T
                 @test mean(marginal) === mean(dist)
                 @test var(marginal) === var(dist)
                 marginal_subscription_flag = true
@@ -70,7 +70,7 @@
                     variable, node_index
                 ),
                 (message) -> begin
-                    @test typeof(message) <: Message{T}
+                    @test typeof(message) <: Message && ReactiveMP.typeofdata(message) <: T
                     @test mean(message) === mean(dist)
                     @test var(message) === var(dist)
                     message_subscription_flag = true
@@ -125,7 +125,7 @@
             (marginals) -> begin
                 @test length(marginals) === length(variables)
                 foreach(marginals) do marginal
-                    @test typeof(marginal) <: Marginal{T}
+                    @test typeof(marginal) <: Marginal && ReactiveMP.typeofdata(marginal) <: T
                     @test mean(marginal) === mean(dist)
                     @test var(marginal) === var(dist)
                 end
@@ -163,7 +163,7 @@
                 (messages) -> begin
                     @test length(messages) === length(variables)
                     foreach(messages) do message
-                        @test typeof(message) <: Message{T}
+                        @test typeof(message) <: Message && ReactiveMP.typeofdata(message) <: T
                         @test mean(message) === mean(dist)
                         @test var(message) === var(dist)
                     end
