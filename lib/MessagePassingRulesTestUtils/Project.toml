name = "MessagePassingRulesTestUtils"
uuid = "08c5795c-a039-451c-8c5c-d65dcf644a97"
version = "0.1.0-DEV"

# Scaffolding only -- Phase 4 fills this in. See PLAN.md.
#
# Consumed via [extras] and the test target, never as a runtime dependency and never as a
# package extension: its quadrature and sampling dependencies must not reach a node
# package's runtime.

[compat]
julia = "1.10"

# Depends on MessagePassingRulesBase. Not declared yet: it is unregistered, and
# [sources] -- the clean way to point at a sibling in a monorepo -- needs Julia 1.11 while
# our floor is 1.10. From the repository root, activate this project and use
# `Pkg.develop(path = "lib/MessagePassingRulesBase")`
# and commits the resulting Manifest. See lib/README.md.
