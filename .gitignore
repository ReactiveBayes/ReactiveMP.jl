**/.ipynb_checkpoints

docs/build
lib/*/docs/build
docs/Manifest.toml

demo/.ipynb_checkpoints
demo/*incomplete.ipynb
demo/*.jl
demo/*WIP*

_output
test/_output

.idea
.vscode

**/*.aux
**/*.log
**/*.synctex.*
**/*.cov
**/*.mem
**/private*

Coverage.ipynb

**/.DS_Store

examples/*Compiled

**/Manifest.toml
**/LocalPreferences.toml
# Tracked so `make format` uses a reproducible Runic version everywhere.
!scripts/Manifest.toml
# Tracked so the v6/v7 comparison resolves to the same versions for everyone. The whole
# point of this environment is that it pins v6; an unpinned Manifest defeats it.
!compat/v6-comparison/Manifest.toml
